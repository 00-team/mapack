// #[cfg(test)]
mod tests {
    mod my_tiles {
        mapack::mapack! {
            poi: {
                name: String,
            },
        }
    }

    #[test]
    fn test() {
        // my_tiles::Tile::decode(zom, tx, ty, pbf)
    }
}
